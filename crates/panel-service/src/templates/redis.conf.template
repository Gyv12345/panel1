# Redis 配置模板
# 由 Panel1 自动生成

# 网络配置
bind 0.0.0.0
port {port}

# 内存配置
maxmemory 256mb
maxmemory-policy allkeys-lru

# 持久化配置
appendonly yes
appendfsync everysec

# 日志配置
loglevel notice

# 安全配置
# requirepass your_password_here
